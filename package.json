{
  "name": "paperwork-assistant",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "services/api",
    "packages/contracts"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "api:dev": "npm run build --workspace @paperwork-assistant/contracts && npm run dev --workspace @paperwork-assistant/api",
    "api:start": "npm run build && npm run start --workspace @paperwork-assistant/api",
    "build": "npm run build --workspace @paperwork-assistant/contracts && npm run build --workspace @paperwork-assistant/api",
    "lint": "npm run lint --workspace @paperwork-assistant/contracts && npm run lint --workspace @paperwork-assistant/api",
    "test": "npm run build --workspace @paperwork-assistant/contracts && npm run test --workspace @paperwork-assistant/contracts && npm run test --workspace @paperwork-assistant/api",
    "typecheck": "npm run build --workspace @paperwork-assistant/contracts && npm run typecheck --workspace @paperwork-assistant/api"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.20.1",
    "eslint": "^10.8.0",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  }
}
